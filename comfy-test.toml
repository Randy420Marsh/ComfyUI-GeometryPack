[test]
levels = ["syntax", "install", "registration", "instantiation", "execution"]

[test.workflows]
timeout = 300
cpu = ["!remeshing_gpu"]
gpu = ["remeshing_gpu"]

[test.platforms]
linux = true
macos = true
windows = true
windows_portable = true
